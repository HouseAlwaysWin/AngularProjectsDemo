import { FormFieldInvalidDirective } from './form-field-invalid.directive';

describe('FormFieldInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
